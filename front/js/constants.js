const CONSTANTS = {
    'WINDOW_HEIGHT': $(window).height(),
    'WINDOW_WIDTH': $(window).width(),
    'SMB_BLOCK': 30 //$(window).width()/16 //Super Mario Bros Block width/height, 16-block-wide screen
}